## What changed

## Register items this satisfies
<!-- e.g. C1, P8. Run `make register` and paste the lines that flipped to ok. -->

## Checks
- [ ] `make check` run locally
- [ ] `make claims` clean — no dead claims introduced
- [ ] If this touches a chapter, `make words` and `make rubric` reviewed
- [ ] If this touches the pre-registration after locking, the change is declared in Methods

## Anything asserted here that isn't in the register or produced by the study?
<!-- If yes, it does not go in. That is the rule. -->
