# Budget options

Generated by `plan/budget_options.py` from the rates in `plan/cost_model.py`.
Regenerate after changing either. **Do not hand-edit.**

Baseline for the current design — two local checkpoints, no API target arms —
is **207,760 episodes, 346 GPU-hours, $212**
(+25% contingency = **$265**).

Levers are ranked by **scientific cost**, not by saving. A cheaper study that
cannot answer its own question is not a saving. Severity 0 is a risk, not a lever.

| Lever | Effect on cost | Left | Severity |
|---|---|---|---|
| GPU provider: RunPod community -> Vast.ai interruptible | $62 | $150 | 1 |
| GPU provider: -> Vast.ai on-demand | $14 | $198 | 1 |
| Throughput turns out to be 450 ep/hr, not 600 | **+$38 more** | $250 | 0 |
| Run the attacker optimiser locally (--provider vllm) | $98 | $114 | **5 — do not** |
| Four seed styles to two | $27 | $185 | 3 |
| Six suites back to four | $95 | $117 | 4 |
| Attacker five rounds to three | $63 | $149 | **5 — do not** |

## What each lever costs, and what it must declare

**GPU provider: RunPod community -> Vast.ai interruptible** — $150 (saves $62), severity 1.
Preemption. Survivable by design: cells are independent and the fingerprint gate re-verifies on restart, so a lost cell is a re-run. But a cell is ~22 GPU-hours, so checkpoint per INJECTION TASK, not per cell, or a late preemption is expensive.
*Declare:* None. Provider choice is not a design change.

**GPU provider: -> Vast.ai on-demand** — $198 (saves $14), severity 1.
Marketplace host quality varies and there is no SLA. With a hard 20 September deadline, verify a host before committing the run.
*Declare:* None.

**Throughput turns out to be 450 ep/hr, not 600** — $250 (costs $38 MORE), severity 0.
Not a lever - a risk. Shown because it moves the GPU line further than any provider choice. Measure one cell before booking.
*Declare:* None.

**Run the attacker optimiser locally (--provider vllm)** — $114 (saves $98), severity 5.
REJECTED, and it is not the saving it looks like. llm_utils.py accepts --provider vllm, so R2 could be made zero-API - it would remove the $98 line entirely. But the optimiser IS the adaptive attacker: 3.5 places this study at the attack-aware tier and the whole RQ2 result is a claim about what an adapting adversary achieves. Substituting an 8B model for the default frontier optimiser cuts attacker CAPABILITY, which biases rho* toward 1 and pushes the study toward its own refutation branch. That is the severity-5 round-cutting lever in a different costume. The adequacy precondition (>=40% ASR undefended) catches a catastrophically weak attacker; it does not catch a merely weaker one.
*Declare:* Would invalidate the attack-aware tier claim in 3.5 and 3.8 criterion 1. Listed only so that it is visibly rejected.

**Four seed styles to two** — $185 (saves $27), severity 3.
R1 is the published-attack lower bound and feeds the adaptive lift, which is RQ3's scientific force. A two-seed baseline is noisier in the quantity RQ3 grades on, and §2.4 criterion 4 is already only partly discharged.
*Declare:* Static regime rests on two published seed styles, not four.

**Six suites back to four** — $117 (saves $95), severity 4.
Clusters drop 49 -> 27, below the range where cluster-robust variance is reliable, and RQ1's discrimination test loses two of its six deployments. Both are load-bearing.
*Declare:* Would invalidate the pre-registered cluster count. Do not.

**Attacker five rounds to three** — $149 (saves $63), severity 5.
The one lever that biases the study toward its own refutation branch, and it risks failing the adequacy precondition outright - which makes an arm uninterpretable rather than merely cheaper.
*Declare:* Would invalidate the locked analysis plan. On the list only so it is visibly rejected.

## Recommendation

**Take the provider lever and nothing else.** The study now costs roughly what
two levers used to save, so the scientific concessions that dominated the old
version are no longer worth making — the model dimension has already been cut to
its floor and the locked plan fixes everything else.

- **Cost-first:** Vast.ai A6000 interruptible, **$150** ($187 with contingency). Checkpoint per injection task.
- **Reliability-first:** RunPod A6000 community, **$212** ($265 with contingency). The default.
- The gap between them is **$62**, which is small enough that
  reliability is worth buying if the run starts inside the final fortnight.

**Buy wall-clock with parallelism, not with faster cards.** Total GPU-hours are
the same however they are split across boxes, so cost is invariant to worker
count while wall-clock divides by it. A faster card costs *more* per episode:
the L40S is ~2x the throughput at ~2.4x the price. Renting three A6000s beats
renting one L40S on both axes.

## Note on where the money is

The GPU line is $114 of $212; the
two API lines — attacker optimiser $98 and camel's quarantined
model $0 — are $98 between them and are
the harder half to cut. The optimiser is the attacker, and cutting it is the
rejected severity-5 lever; camel's quarantined calls are 4 of 8 cells and
cutting them removes the only deterministic axis.

**The largest uncertainty is not a price.** `GPU_EPISODES_HR = 600` is still
assumed. At 450 ep/hr the GPU line moves further than the whole spread between
the cheapest and dearest provider in the table. Measure one cell before booking.
